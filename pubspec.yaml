name: go_router_potd
description: "Deep dive flutter packages. #PackageOfTheDay"
publish_to: 'none' 

version: 1.0.0+1

environment:
  sdk: '>=3.2.0-65.0.dev <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0
  go_router: ^10.0.0

flutter:
  uses-material-design: true